name: IndexNow Submit

# Submits all sitemap URLs to IndexNow after a production deploy succeeds, so
# search engines (Bing, Yandex, …) are notified the moment new/updated content
# is actually live — including the verification key file the deploy ships.
#
# Triggered by the GitHub "deployment_status" event that Vercel emits. We only
# act on a successful *Production* deployment (not preview builds).

on:
  deployment_status:

jobs:
  submit:
    if: >-
      github.event.deployment_status.state == 'success' &&
      github.event.deployment_status.environment == 'Production'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      # Build so scripts/prerender.ts regenerates dist/sitemap.xml with the
      # current route table and canonical domain before we submit it.
      - name: Install dependencies
        run: npm ci

      - name: Build (generates dist/sitemap.xml)
        run: npm run build

      # scripts/indexnow.mjs reads dist/sitemap.xml and POSTs to
      # api.indexnow.org using only Node built-ins (global fetch + fs).
      - name: Submit URLs to IndexNow
        run: node scripts/indexnow.mjs
